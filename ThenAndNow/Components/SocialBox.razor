<div class="d-flex flex-column">
    <div class="d-flex justify-content-between align-content-center mb-3">
        <div class="d-flex flex-wrap gap-2">
            @foreach (var buttonType in SocialButtons)
            {
                <ShareButton Type="buttonType" Url="@DirectUrl" />
            }
        </div>
        <button class="btn btn-sm btn-dark" @onclick="ToggleReplies">@(ShowReplies? Labels.HideReplies: Labels.ShowReplies)</button>
    </div>
    <div class="d-flex flex-column">
        @if (ShowReplies)
        {
            <Loader DataLoaded="DataLoaded">
                @if (Replies.Any())
                {
                    <div class="@ReplyBoxClass" style="@ReplyBoxStyle">
                        @*<Virtualize Context="reply" Items="Replies" ItemSize="500">
                            <ReplyComponent Reply="reply" @key="reply.Id" />
                        </Virtualize> *@
                        @foreach (var reply in Replies)
                        {
                            <ReplyComponent Reply="reply" @key="reply.Id" />
                        }
                    </div>
                    <div class="d-flex justify-content-end">
                        <button class="btn btn-sm btn-dark" @onclick="AddReply">@Labels.AddReply</button>
                    </div>
                }
                else
                {
                    <div class="d-flex justify-content-center gap-2">
                        <button class="btn btn-sm btn-danger" @onclick="AuthUser">AuthUser</button>
                        <button class="btn btn-sm btn-dark" @onclick="AddReply">@Labels.AddFirstReply</button>
                    </div>
                }
            </Loader>
        }
    </div>
</div>